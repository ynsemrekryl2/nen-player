// NEN-011 JVM ölçüm harness'ı — ÜRÜN KODU DEĞİLDİR (CLAUDE.md kural 7).
// Swift'teki `apple-harness/Package.swift`'in Gradle karşılığı; ürün
// `platforms/android/`'ı tanımaz — ADR-0028'in birinci sınırı.

rootProject.name = "spike-async-cancel-jvm-harness"
